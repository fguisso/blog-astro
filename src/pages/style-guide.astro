---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import GlowBox from '../components/GlowBox.astro';
import GlowButton from '../components/GlowButton.astro';
import Header from '../components/Header.astro';
import Callout from '../components/Callout.astro';
import FeaturedProjectCard from '../components/FeaturedProjectCard.astro';
import TalkAvatarBadge from '../components/TalkAvatarBadge.astro';
import LogoWordmark from '../components/LogoWordmark.astro';
import PostCard from '../components/PostCard.astro';
import Chip from '../components/Chip.astro';
import { themeConfig } from '../theme.config';
import { routes } from '../utils/routes';

const palette = [
	{ name: 'Fundo base', token: '--color-bg', value: '#020204' },
	{ name: 'Painel', token: '--color-panel', value: '#131318' },
	{ name: 'Primário', token: '--color-fg', value: '#F5F5FF' },
	{ name: 'Texto secundário', token: '--color-muted', value: '#A8A9BB' },
	{ name: 'Neon verde', token: '--color-accent', value: '#00FF7F' },
	{ name: 'Neon roxo', token: '--color-secondary', value: '#9A1AFF' },
	{ name: 'Neon azul', token: '--color-highlight', value: '#6E6EFF' },
];

const typography = [
	{
		label: 'Hero / H1',
		stack: 'Space Grotesk Bold · clamp(48px, 6vw, 72px)',
		description: 'H1 e heros. Usa gradientes apenas se aplicar span.primary dentro do texto.',
	},
	{
		label: 'Seções / H2–H3',
		stack: 'Space Grotesk SemiBold · 32–40px',
		description: 'Títulos internos e seções. Sempre em Space Grotesk para manter o ritmo.',
	},
	{
		label: 'Body',
		stack: 'Inter Regular · 18px · line-height 1.7',
		description: 'Parágrafos, descrições e notas de cards.',
	},
	{
		label: 'Highlight span',
		stack: 'Oferta do Dia + span.primary',
		description: 'Apenas para trechos curtos (badges, palavras-chave, CTA dentro do texto).',
	},
];

const buttonVariants = [
	{ variant: 'primary', label: 'Primary', usage: 'Link discreto dentro de cards e textos.' },
	{ variant: 'accent', label: 'Accent', usage: 'Principal CTA neon para páginas e formulários.' },
	{ variant: 'secondary', label: 'Secondary', usage: 'Links de apoio, sem roubar atenção.' },
	{ variant: 'outline', label: 'Outline', usage: 'CTA com fundo transparente em áreas escuras.' },
	{ variant: 'ghost', label: 'Ghost', usage: 'Botões utilitários, como filtros e anchors.' },
 ] as const;

const gradientSwatches = [
	{
		label: 'Glow mesh',
		token: '--glow-gradient',
		description: 'Plano de fundo global. Mistura verde, roxo e azul em radial blur.',
		variant: 'mesh',
	},
	{
		label: 'Primary beam',
		token: 'span.primary',
		description: 'Texto com gradiente padrão para headlines e destaques.',
		variant: 'beam',
	},
	{
		label: 'Neon shield',
		token: 'GlowBox::before',
		description: 'Borda iluminada dos componentes estruturais.',
		variant: 'shield',
	},
];

const postSamples = [
	{
		title: 'Guardrails neon para o pipeline de AppSec',
		description: 'Como integrei scanners, alerts e feature flags em um único fluxo sem travar deploys.',
		dateLabel: '24 jun 2024',
		stats: '1.1k palavras · 6 min',
		tag: 'AppSec',
		image: 'https://images.unsplash.com/photo-1520607162513-77705c0f0d4a?auto=format&fit=crop&w=1400&q=80',
	},
	{
		title: 'Home lab como staging de autenticação',
		description: 'Orquestração de VMs e tunéis WireGuard para validar fluxos sem depender do ambiente oficial.',
		dateLabel: '18 mai 2024',
		stats: '980 palavras · 5 min',
		tag: 'Home-lab',
		image: 'https://images.unsplash.com/photo-1518770660439-4636190af475?auto=format&fit=crop&w=1400&q=80',
	},
	{
		title: 'Design tokens de neon para Figma e código',
		description: 'Documentei como manter as mesmas cores e espaçamentos em todas as superfícies.',
		dateLabel: '09 abr 2024',
		stats: '760 palavras · 4 min',
		tag: 'Design',
		image: 'https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?auto=format&fit=crop&w=1400&q=80',
	},
];

const logoMarks = [
	{
		label: 'Monograma G',
		context: 'Badge micro para scripts e GTuner.',
		variant: 'mono',
		props: { label: 'g', showRightMarker: false, width: 110 },
	},
	{
		label: 'gwiki wordmark',
		context: 'wiki.guisso.dev',
		variant: 'wiki',
		props: { label: 'gwiki', width: 200, showRightMarker: false },
	},
	{
		label: 'gtuner wordmark',
		context: 'guisso.dev/gtuner',
		variant: 'tuner',
		props: { label: 'gtuner', width: 220, showRightMarker: false },
	},
];

const { site, featuredProjects } = themeConfig;
const projectSamples = featuredProjects.slice(0, 2);
const projectsHref = routes.projects();
const samplePostHref = routes.blogPost('exemplo');
---

<!doctype html>
<html lang="pt-BR">
	<head>
		<BaseHead title={`Style Guide · ${site.title}`} description="Referência visual com os componentes neon usados no blog." />
	</head>
	<body>
		<Header />
		<main class="page-shell style-guide-shell">
				<section class="style-guide__hero">
					<p class="eyebrow">style guide</p>
					<h1 class="display-headline">
						<span class="display-special primary">Todos os</span>
						<span>blocos prontos do kit neon</span>
					</h1>
					<p>
						Esta página concentra os componentes reais do blog para facilitar revisões, QA e criação de novos layouts.
						Tudo aqui reutiliza o mesmo tema escuro com gradientes neon, sem importar libs externas.
					</p>
					<p class="style-guide__hero-note">
						Precisa criar uma nova seção ou componente? Duplique o bloco correspondente daqui, ajuste o conteúdo e mantenha os mesmos tokens.
						Quando precisar da antiga fonte Oferta do Dia, use <code>&lt;span class="display-special primary"&gt;</code> para aplicar o gradiente padrão.
					</p>
				<div class="style-guide__hero-cta">
					<GlowButton href="#componentes" variant="accent" size="lg">Ver componentes</GlowButton>
					<GlowButton href={projectsHref} variant="ghost" size="lg">Projetos que usam o kit</GlowButton>
				</div>
			</section>

			<section class="style-guide__section" id="cores">
				<div class="style-guide__section-head">
					<p class="eyebrow">cores</p>
					<h2>Paleta oficial e tokens</h2>
					<p>Use sempre os tokens abaixo. Eles já possuem contraste ideal para o fundo escuro e respondem ao mesmo gradiente neon.</p>
				</div>
				<GlowBox accent="highlight" class="style-guide__panel">
					<ul class="palette-grid">
						{palette.map((color) => (
							<li class="palette-card">
								<span class="palette-swatch" style={`background: ${color.token.startsWith('--color') ? `var(${color.token})` : color.value};`}></span>
								<p class="palette-name">{color.name}</p>
								<p class="palette-token">{color.token}</p>
								<p class="palette-value">{color.value}</p>
							</li>
						))}
					</ul>
				</GlowBox>
			</section>

			<section class="style-guide__section" id="gradientes">
				<div class="style-guide__section-head">
					<p class="eyebrow">gradientes</p>
					<h2>Como o brilho é aplicado</h2>
					<p>
						Os três gradientes principais mantêm o neon consistente em fundos, textos e contornos. Combine-os com <code>span.primary</code> para
						aplicar o mesmo feixe usado na home.
					</p>
				</div>
				<GlowBox accent="accent" class="style-guide__panel gradient-panel">
					<ul class="gradient-grid">
						{gradientSwatches.map((item) => (
							<li class={`gradient-card gradient-card--${item.variant}`}>
								<div class="gradient-card__preview"></div>
								<div class="gradient-card__body">
									<p class="gradient-card__label">{item.label}</p>
									<p class="gradient-card__token">{item.token}</p>
									<p class="gradient-card__description">{item.description}</p>
								</div>
							</li>
						))}
					</ul>
				</GlowBox>
			</section>

			<section class="style-guide__section" id="tipografia">
				<div class="style-guide__section-head">
					<p class="eyebrow">tipografia</p>
					<h2>Escala tipográfica</h2>
					<p>Space Grotesk passou a liderar os títulos, Inter segura os textos e Oferta do Dia fica reservada a spans especiais.</p>
				</div>
				<GlowBox accent="secondary" padding="relaxed" class="style-guide__panel">
					<div class="type-grid">
						{typography.map((token) => (
							<div class="type-card">
								<p class="type-label">{token.label}</p>
								<p class="type-stack">{token.stack}</p>
								<p class="type-description">{token.description}</p>
							</div>
						))}
					</div>
				</GlowBox>
				<GlowBox accent="highlight" class="style-guide__panel type-samples">
					<div>
						<p class="eyebrow">Heading 1</p>
						<h1 class="display-headline">Heading 1 · Space Grotesk 64px</h1>
					</div>
					<div>
						<p class="eyebrow">Heading 2</p>
						<h2 class="display-headline">Heading 2 · Space Grotesk 40px</h2>
					</div>
					<div>
						<p class="eyebrow">Heading 3</p>
						<h3 class="display-headline">Heading 3 · Space Grotesk 28px</h3>
					</div>
					<div>
						<p class="eyebrow">Paragraph</p>
						<p>
							Esta é a base: Inter 18px, line-height 1.7. Use <span class="display-special primary">span.primary</span> para o
							highlight com Oferta do Dia quando precisar de contraste.
						</p>
					</div>
				</GlowBox>
			</section>

			<section class="style-guide__section" id="componentes">
				<div class="style-guide__section-head">
					<p class="eyebrow">componentes</p>
					<h2>Botões e estados</h2>
					<p>GlowButton cobre o espectro inteiro de CTAs. Combine variantes e tamanhos para qualquer cenário.</p>
				</div>
				<GlowBox accent="accent" class="style-guide__panel">
					<div class="button-grid">
						{buttonVariants.map((btn) => (
							<div class="button-card">
								<GlowButton variant={btn.variant}>{btn.label}</GlowButton>
								<p>{btn.usage}</p>
							</div>
						))}
					</div>
					<div class="button-grid button-grid--sizes">
						<GlowButton size="sm" variant="ghost">Compacto</GlowButton>
						<GlowButton size="md" variant="primary">Padrão</GlowButton>
						<GlowButton size="lg" variant="accent">Expresso</GlowButton>
					</div>
					</GlowBox>
				</section>

				<section class="style-guide__section" id="chips">
					<div class="style-guide__section-head">
						<p class="eyebrow">labels</p>
						<h2>Chips redondas</h2>
						<p>Componente Chip concentra tags redondas dos cards de posts e filtros. Variante padrão é ghost.</p>
					</div>
					<GlowBox accent="highlight" class="chip-panel">
						<div class="chip-grid">
							<Chip>AppSec</Chip>
							<Chip>Home Lab</Chip>
							<Chip>Infra</Chip>
							<Chip variant="solid">Release</Chip>
						</div>
						<p class="chip-panel__note">
							Use <code>&lt;Chip variant="ghost"&gt;</code> nos cards (matching blog) e <code>variant="solid"</code> para estados que precisam de mais contraste.
						</p>
					</GlowBox>
				</section>

			<section class="style-guide__section" id="layouts">
				<div class="style-guide__section-head">
					<p class="eyebrow">blocos base</p>
					<h2>GlowBox + Callout</h2>
					<p>
						GlowBox organiza painéis e clusters. Dentro dele cabem callouts, listas e qualquer conteúdo editorial mantendo o glow suave.
					</p>
				</div>
				<div class="layout-grid">
					<GlowBox accent="accent" padding="relaxed" class="layout-card">
						<h3>GlowBox relaxado</h3>
						<p>
							Use <code>padding=&quot;relaxed&quot;</code> para hero secundário ou para destacar blocos densos como changelogs. A borda sempre usa o mesmo gradiente neon.
						</p>
						<ul>
							<li>Accent: destaque principal.</li>
							<li>Secondary: contrasta com roxo.</li>
							<li>Highlight: mistura azul e roxo.</li>
						</ul>
					</GlowBox>
					<GlowBox accent="secondary" padding="snug" class="layout-card">
						<h3>Callout embutido</h3>
						<p>
							Callout entra direto no GlowBox para notas e avisos. Cada variante comunica um estado.
						</p>
						<Callout title="Info padrão" variant="info">
							<p>Mantém o foco no tom verde neon com contraste máximo.</p>
						</Callout>
						<Callout title="Sucesso" variant="success">
							<p>Confirmações ou desbloqueios de features.</p>
						</Callout>
						<Callout title="Atenção" variant="warning">
							<p>Alertas suaves, nunca use vermelho.</p>
						</Callout>
					</GlowBox>
				</div>
			</section>

			<section class="style-guide__section" id="cards">
				<div class="style-guide__section-head">
					<p class="eyebrow">cards</p>
					<h2>FeaturedProjectCard em ação</h2>
					<p>Os cards neon usam imagem de fundo com blend modes e CTA em caps. Basta fornecer título, descrição, url e imagem.</p>
				</div>
				<GlowBox accent="highlight" class="style-guide__panel">
					<ul class="featured-projects__grid featured-projects__grid--showcase">
						{projectSamples.map((project) => (
							<FeaturedProjectCard project={project} />
						))}
					</ul>
				</GlowBox>
			</section>

			<section class="style-guide__section" id="post-cards">
				<div class="style-guide__section-head">
					<p class="eyebrow">posts</p>
					<h2>Cards do blog</h2>
					<p>Grid neon para artigos: mantém meta de leitura, tag e CTA ghost alinhados ao restante do sistema.</p>
				</div>
				<GlowBox accent="secondary" class="style-guide__panel">
					<ul class="featured-projects__grid featured-projects__grid--posts">
						{postSamples.map((post) => (
							<PostCard
								href={samplePostHref}
								title={post.title}
								description={post.description}
								statsLabel={post.stats}
								tag={post.tag}
								image={post.image}
								ctaLabel="ler artigo ↗"
							>
								<span slot="label">{post.dateLabel}</span>
							</PostCard>
						))}
					</ul>
				</GlowBox>
			</section>

			<section class="style-guide__section" id="avatar">
				<div class="style-guide__section-head">
					<p class="eyebrow">identidade</p>
					<h2>Avatar duplo (TalkAvatarBadge)</h2>
					<p>Mesmo badge que aparece no header: duas fotos empilhadas criam um efeito parallax quando o cursor se aproxima.</p>
				</div>
				<GlowBox accent="highlight" class="avatar-panel">
					<div class="avatar-panel__preview">
						<TalkAvatarBadge />
						<p>
							O hover desloca o avatar secundário e dá sensação de holograma. Troque imagens via props <code>avatarSrc</code> e <code>photoSrc</code>.
						</p>
					</div>
					<p class="avatar-panel__note">
						Ideal para CTAs pessoais, seções “sobre” e convites para talks — mantém o mesmo glow e não usa libs externas.
					</p>
				</GlowBox>
			</section>

			<section class="style-guide__section" id="logo">
				<div class="style-guide__section-head">
					<p class="eyebrow">logo</p>
					<h2>Versões aprovadas</h2>
					<p>LogoWordmark responde à cor do container. Use o fundo certo para alternar automaticamente entre branco e preto.</p>
					<p class="logo-note">
						Para microsites mantemos variações tipográficas: <strong>gwiki</strong> (wiki.guisso.dev), <strong>gtuner</strong> (guisso.dev/gtuner) e o
						monograma <strong>G</strong> para ícones compactos.
					</p>
				</div>
				<div class="logo-grid">
					<div class="logo-swatch logo-swatch--dark">
						<p>Versão branca (default)</p>
						<LogoWordmark class="logo-sample" aria-hidden="true" />
					</div>
					<div class="logo-swatch logo-swatch--light">
						<p>Versão preta</p>
						<LogoWordmark class="logo-sample logo-sample--dark" aria-hidden="true" />
					</div>
				</div>
				<div class="logo-mark-grid">
					{logoMarks.map((mark) => (
						<div class={`logo-mark logo-mark--${mark.variant}`}>
							<p class="logo-mark__label">{mark.label}</p>
							<p class="logo-mark__context">{mark.context}</p>
							<LogoWordmark class="logo-mark__glyph" {...mark.props} aria-hidden="true" />
						</div>
					))}
				</div>
			</section>

		</main>
		<Footer />
	</body>
</html>

<style>
	.style-guide-shell {
		padding-top: 4rem;
		padding-bottom: 5rem;
		display: flex;
		flex-direction: column;
		gap: 3.5rem;
	}

	.style-guide__hero {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		max-width: 780px;
	}

	.style-guide__hero h1 {
		margin-bottom: 0.5rem;
	}

	.style-guide__hero-note {
		color: var(--color-muted);
		margin: 0;
	}

	.style-guide__hero-cta {
		display: flex;
		flex-wrap: wrap;
		gap: 1rem;
		margin-top: 0.5rem;
	}

	.style-guide__section {
		display: flex;
		flex-direction: column;
		gap: 1.25rem;
	}

	.style-guide__section-head {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
		max-width: 720px;
	}

	.style-guide__panel {
		width: 100%;
	}

	.palette-grid {
		list-style: none;
		margin: 0;
		padding: 0;
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
		gap: 1rem;
	}

	.palette-card {
		background: rgba(255, 255, 255, 0.02);
		border-radius: 1rem;
		padding: 1rem;
		border: 1px solid rgba(255, 255, 255, 0.08);
		display: flex;
		flex-direction: column;
		gap: 0.35rem;
	}

	.palette-swatch {
		display: block;
		width: 100%;
		height: 110px;
		border-radius: 0.9rem;
		border: 1px solid rgba(255, 255, 255, 0.15);
		box-shadow: inset 0 0 30px rgba(0, 0, 0, 0.35);
	}

	.palette-name {
		margin: 0.5rem 0 0;
		font-weight: 600;
		color: var(--color-fg);
	}

	.palette-token {
		margin: 0;
		font-size: 0.85rem;
		color: var(--color-muted);
	}

	.palette-value {
		margin: 0;
		font-size: 0.9rem;
		color: var(--color-highlight);
	}

	.type-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
		gap: 1.5rem;
	}

	.type-card {
		display: flex;
		flex-direction: column;
		gap: 0.35rem;
	}

	.type-label {
		margin: 0;
		font-weight: 600;
		color: var(--color-fg);
	}

	.type-stack {
		margin: 0;
		font-family: 'Didact Gothic', 'Inter', sans-serif;
		font-size: 1.2rem;
		color: var(--color-accent);
	}

	.type-description {
		margin: 0;
		color: var(--color-muted);
	}

	.button-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
		gap: 1rem;
	}

	.button-card {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
		background: rgba(255, 255, 255, 0.01);
		border: 1px solid rgba(255, 255, 255, 0.08);
		border-radius: 1rem;
		padding: 1rem;
	}

	.button-card p {
		margin: 0;
		color: var(--color-muted);
		font-size: 0.95rem;
	}

	.button-grid--sizes {
		grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
		margin-top: 1.5rem;
	}

	.layout-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 1.5rem;
	}

	.layout-card h3 {
		margin-top: 0;
	}

	.layout-card p {
		color: var(--color-muted);
		margin-bottom: 1rem;
	}

	.layout-card ul {
		margin: 0;
		padding-left: 1.25rem;
		color: var(--color-muted);
	}

	.featured-projects__grid--showcase {
		margin-top: 0.5rem;
	}

	.gradient-grid {
		list-style: none;
		padding: 0;
		margin: 0;
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
		gap: 1rem;
	}

	.gradient-card {
		display: flex;
		gap: 1rem;
		padding: 1rem;
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: 1rem;
		align-items: center;
		background: rgba(255, 255, 255, 0.02);
	}

	.gradient-card__preview {
		width: 72px;
		height: 72px;
		border-radius: 0.9rem;
		border: 1px solid rgba(255, 255, 255, 0.2);
		box-shadow: inset 0 0 20px rgba(0, 0, 0, 0.4);
	}

	.gradient-card--mesh .gradient-card__preview {
		background-image: var(--glow-gradient);
	}

	.gradient-card--beam .gradient-card__preview {
		background-image: linear-gradient(
			120deg,
			var(--color-accent),
			var(--color-secondary),
			var(--color-highlight)
		);
	}

	.gradient-card--shield .gradient-card__preview {
		background-image: linear-gradient(135deg, rgba(0, 255, 127, 0.35), rgba(154, 26, 255, 0.35));
	}

	.gradient-card__body {
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
	}

	.gradient-card__label {
		margin: 0;
		font-weight: 600;
		color: var(--color-fg);
	}

	.gradient-card__token {
		margin: 0;
		font-size: 0.8rem;
		text-transform: uppercase;
		letter-spacing: 0.2em;
		color: var(--color-muted);
	}

	.gradient-card__description {
		margin: 0;
		color: var(--color-muted);
	}
	.gradient-panel {
		padding: 1.5rem;
	}

	.chip-panel {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.chip-grid {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
	}

	.chip-panel__note {
		margin: 0;
		color: var(--color-muted);
		font-size: 0.95rem;
	}

	.type-samples {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
		gap: 1.25rem;
	}

	.type-samples > div {
		display: flex;
		flex-direction: column;
		gap: 0.35rem;
	}

	.type-samples h1,
	.type-samples h2,
	.type-samples h3 {
		margin: 0;
	}

	.type-samples p {
		margin: 0;
		color: var(--color-muted);
	}

	.avatar-panel {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.avatar-panel__preview {
		display: flex;
		align-items: center;
		gap: 1.25rem;
		flex-wrap: wrap;
	}

	.avatar-panel__preview p {
		margin: 0;
		color: var(--color-muted);
		max-width: 420px;
	}

	.avatar-panel__note {
		margin: 0;
		color: var(--color-muted);
		font-size: 0.95rem;
	}

	.logo-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
		gap: 1.25rem;
	}

	.logo-swatch {
		border-radius: 1.25rem;
		padding: 1.5rem;
		border: 1px solid rgba(255, 255, 255, 0.08);
		display: flex;
		flex-direction: column;
		gap: 1rem;
		box-shadow: var(--shadow-soft);
	}

	.logo-swatch p {
		margin: 0;
		font-weight: 600;
	}

	.logo-swatch--dark {
		background: #050507;
		color: var(--color-fg);
	}

	.logo-swatch--light {
		background: #f5f5ff;
		color: #050505;
	}

	.logo-sample {
		color: currentColor;
	}

	.logo-sample--dark {
		color: #050505;
	}

	.logo-note {
		margin: 0.4rem 0 0;
		color: var(--color-muted);
	}

	.logo-mark-grid {
		margin-top: 1.5rem;
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
		gap: 1rem;
	}

	.logo-mark {
		padding: 1.25rem;
		border-radius: 1.25rem;
		border: 1px solid rgba(255, 255, 255, 0.08);
		background: rgba(255, 255, 255, 0.02);
		display: flex;
		flex-direction: column;
		gap: 0.4rem;
	}

	.logo-mark__label {
		margin: 0;
		font-weight: 600;
	}

	.logo-mark__context {
		margin: 0;
		color: var(--color-muted);
		font-size: 0.9rem;
	}

	.logo-mark__glyph {
		display: block;
		width: 180px;
		color: currentColor;
	}

	.logo-mark--mono .logo-mark__glyph {
		width: 120px;
	}

	.logo-mark--wiki {
		color: var(--color-accent);
	}

	.logo-mark--tuner {
		color: var(--color-highlight);
	}

	@media (max-width: 640px) {
		.style-guide-shell {
			padding-top: 3rem;
		}

		.style-guide__hero-cta {
			flex-direction: column;
		}
	}
</style>
