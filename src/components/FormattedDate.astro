---
interface Props {
	date: Date;
	locale?: string;
}

const { date, locale = 'pt-BR' } = Astro.props as Props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(locale, {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
